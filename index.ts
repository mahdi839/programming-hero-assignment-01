// Single-file TypeScript example (root)
// You can run this directly with `npm run dev-file` which uses ts-node

function add(a: number, b: number): number {
    return a + b;
}

console.log(`5 + 7 = ${add(5, 7)}`);
